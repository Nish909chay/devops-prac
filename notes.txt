What do we want to automate:
    - on every push - auto run pytest


Interview Qns
"Walk me through a CI/CD pipeline you've built." → Use YOUR actual repo. Show the Actions tab.
"What's the difference between CI and CD?"
"How do you handle secrets in a pipeline?"
"What would you do if a deployment breaks production?"
"Explain what a Dockerfile does, line by line" → use your actual Dockerfile.
"What's Infrastructure as Code, and why use it?"
"How would you set up staging vs production environments in a pipeline?"


CI vs CD vs Continuous Deployment (1 line each)
Your pipeline's stages, in order
Docker: image vs container, why containerize
Secrets management: never hardcode, use vault/secrets manager/CI secrets
IaC: what it is, Terraform plan vs apply
Deployment strategies: blue-green, canary, rolling
Rollback: how it works
One sentence you can say confidently: "I built and containerized a small app 
with a GitHub Actions pipeline that runs tests, builds a Docker image, and pushes 
it to Docker Hub on every push to main."